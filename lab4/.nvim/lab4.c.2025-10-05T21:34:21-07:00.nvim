#define _GNU_SOURCE
#include <stdlib.h>
#include <unistd.h>
#include <stdint.h>
#include <stdio.h>

struct header{
  uint64_t size;
  struct header* next;
};

int main() {
  int *heap_ptr=calloc(1, sizeof(int));
  printf("heap_ptr address: %p\n", &heap_ptr);
  return 0;
}
