#define _GNU_SOURCE
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

struct header {
  uint64_t size;
  struct header *next;
};

int main() {
  int *block_start = sbrk(256);
  printf("first block: %p\n", block_start);
  printf("block value: %d\n", 256);
  return 0;
}
